BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[UserTest] (
    [User_id] INT NOT NULL IDENTITY(1,1),
    [Username] NVARCHAR(1000) NOT NULL,
    CONSTRAINT [UserTest_pkey] PRIMARY KEY CLUSTERED ([User_id])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
